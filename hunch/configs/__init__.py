"""Bundled config files shipped as package data.

`sonnet_claude_cli.toml` is the default critic config used by `hunch run` when no
`--critic`/`--config` is given — so a fresh project gets a real critic without
needing a config file of its own or an API key.
"""
